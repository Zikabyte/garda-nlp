# Data Processing
lxml>=4.9.0          # parsing the PAN-2012 dataset
scikit-learn>=1.3.0  # train_test_split, metrics (F1, confusion matrix)
 
# Model Training
setfit>=1.0.3
transformers<5.0     # setfit 1.1.3 doesn't support transformers 5.x yet (breaks on import)
sentence-transformers>=2.2.2
datasets>=2.14.0     # dataset format
torch>=2.0.0         # backend for sentence-transformers
 
# Demo interface
gradio>=4.0.0

# Extension backend
fastapi>=0.110.0
uvicorn>=0.29.0

# Utils
huggingface_hub>=0.19.0
python-dotenv>=1.0.0
